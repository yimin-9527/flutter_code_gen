//通知
import 'package:flutter/material.dart';

class MyMe extends StatelessWidget {
  const MyMe({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('MyMe'));
  }
}
