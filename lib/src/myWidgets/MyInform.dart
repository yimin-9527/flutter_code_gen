//通知
import 'package:flutter/material.dart';

class MyInform extends StatelessWidget {
  const MyInform({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('MyInform'));
  }
}
